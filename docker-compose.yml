version: "3"
services:
  api:
    container_name: api
    build:
      context: .docker
      dockerfile: Dockerfile
    tty: true
    ports:
      - "3000:3000"
    volumes:
      - type: bind
        source: .
        target: /api